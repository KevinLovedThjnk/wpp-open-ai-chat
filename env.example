# WPP Open Apps Facade Configuration
# Copy this file to .env.local and update with your environment

# Staging Environment (default)
VITE_APPS_FACADE_BASE=https://apps-facade-api-ch-stage.os-dev.io

# Production Environment (when ready)
# VITE_APPS_FACADE_BASE=https://apps-facade-api-prd-one.os.wpp.com

# Development/Testing (local)
# VITE_APPS_FACADE_BASE=http://localhost:8080

# Note: This app gets user tokens from Open OS context, not from environment variables
# The token is automatically injected when running within the Open OS platform
